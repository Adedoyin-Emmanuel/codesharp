import * as vscode from "vscode";
import {
  CREATE_CLASS_COMMAND,
  CREATE_ENUM_COMMAND,
  CREATE_FILE_COMMAND,
  CREATE_INTERFACE_COMMAND,
  CREATE_RECORD_COMMAD,
  CREATE_STRUCT_COMMAND,
} from "./constants";

export function activate(context: vscode.ExtensionContext) {
}

export function deactivate() {}
